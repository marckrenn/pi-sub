---
"@marckrenn/pi-sub-core": patch
"@marckrenn/pi-sub-bar": patch
"@marckrenn/pi-sub-shared": patch
"@marckrenn/pi-sub-status": patch
---

Ship `tsconfig.base.json` inside each published package so pi can load npm extensions without a monorepo checkout.
